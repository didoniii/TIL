CommeandProcess는 인터페이스인데 
인터페이스를 생성해서 추상메서드를 정의한다.
추상 메서드는 정의만 되고 구조는 정의되지 않은 것이다.

그리고 클래스를 생성해서 인터페이스를 추가해준다. implements해서

작성하면 에러가 나는데 인터페이스 요소들을 정의해주면된다.

Add unimplemented methods를 클릭하면 자동 생성된다

자동으로 @Override가 생성된다.